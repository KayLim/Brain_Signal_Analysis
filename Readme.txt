Project

Each folder have 10 test set files for aMCI(amenestic mild cognitive impairement) and normal populations, resepctively. 
All files are .set file format compatible with EEGLAB on MATLAB. All files are already manually denoised by ICA and epoch rejections. 
You can directly apply any kinds of feature extractions on the set files. 
